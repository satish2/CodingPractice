//compare nuts and bolts in different ranges
#include<stdio.h>
#include<stdlib.h>
int min[2],max[2];
min[]={1,4};
max[]={3,6};
int main()
{
//if max of one in greater than min of other then ok
//if max of someone is lesser than min of other then "not ok"	
 //compare both minimums and then both maximums if they are not intersecting then leave it 


	return 0;
}	